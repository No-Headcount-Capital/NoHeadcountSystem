# 算法交易系统完整使用指南

这份文档是给你用的“总览版”：  
你可以直接看到**每个脚本做什么、怎么用、系统如何跑起来**。

---

## 1. 先看系统全流程

当前新架构在 `src/` 下，核心流程是：

```text
行情(Tick) -> Strategy(出Signal) -> OMS(转Order) -> RMS(风控校验) -> Gateway(发交易所)
                                                        ^                 |
                                                        |                 v
                                                   成交回报(Trade) <- EventEngine
```

职责分工：
- Strategy：只做信号，不直接下单
- OMS：统一把 Signal 转成 Order，并路由给网关
- RMS：统一风控
- Gateway：只负责和交易所通信
- EventEngine：系统总线，所有模块靠事件连接

---

## 2. 每个脚本是什么、怎么用

## 2.1 运行入口

### `src/main.py`
- 用途：系统启动入口
- 做的事：
  - 自动加载 `.env` / `.env.local`
  - 选择运行模式（`SIM` 或 `BINANCE`）
  - 初始化 EventEngine、Gateway、Risk、OMS、Strategy
  - 启动主循环
- 你怎么用：
  - 直接在 IDE 里运行这个文件即可（一键）

## 2.2 基础设施层（infrastructure）

### `src/infrastructure/events.py`
- 用途：定义事件类型常量与 `Event` 对象
- 事件类型：
  - `eTick` 行情
  - `eSignal` 策略信号
  - `eOrder` 订单状态
  - `eTrade` 成交回报
- 你怎么用：一般不直接改，只在新增事件类型时改

### `src/infrastructure/event_engine.py`
- 用途：事件引擎（队列 + 分发）
- 关键接口：
  - `start()/stop()`
  - `register(event_type, handler)`
  - `put(Event(...))`
- 你怎么用：
  - 写新模块时，注册事件处理器即可

### `src/infrastructure/models.py`
- 用途：统一数据模型
- 主要对象：
  - `TickData` 行情
  - `Signal` 策略信号
  - `OrderData` 订单
  - `TradeData` 成交
- 你怎么用：
  - 策略发单只创建 `Signal`
  - OMS/Gateway 使用 `OrderData`、`TradeData`

### `src/infrastructure/gateway.py`
- 用途：交易所连接与下单网关
- 包含两个实现：
  - `SimulatedGateway`：模拟撮合，开发测试用
  - `BinanceTestnetGateway`：Binance 测试网
- 你怎么用：
  - 不直接调用，`main.py` 会按环境变量自动选择

### `src/infrastructure/oms.py`
- 用途：订单管理系统（OMS）
- 做的事：
  - 监听 `eSignal`
  - 调用 RMS 进行风控
  - 通过 Gateway 发单
  - 跟踪订单状态与信号映射
- 你怎么用：
  - 通常不手动调用，系统初始化后自动工作

## 2.3 风控层（risk）

### `src/risk/manager.py`
- 用途：风控管理器（RMS）
- 默认风控：
  - 单笔最大下单量 `max_order_volume`
  - 品种净仓位限制 `max_symbol_position`
- 你怎么用：
  - 在 `main.py` 里修改 `RiskConfig(...)`

## 2.4 策略层（strategies）

### `src/strategies/base.py`
- 用途：策略基类
- 关键点：
  - 自动监听 Tick/Order/Trade
  - 通过 `emit_signal(...)` 发信号
  - 子类只实现 `on_tick(...)` 即可
- 你怎么用：
  - 写新策略时继承这个类

### `src/strategies/grid.py`
- 用途：示例网格策略
- 当前逻辑：
  - 价格 <= 下沿：发买入信号
  - 价格 >= 上沿：发卖出信号
- 你怎么用：
  - 直接运行即可看到网格策略行为
  - 想改策略，就先从这个文件改

### `src/strategies/cep.py`
- 用途：策略容器（CEP Engine）
- 做的事：
  - 注册多个策略
  - 统一 `start()/stop()`
- 你怎么用：
  - 需要多策略并行时，在 `main.py` 里多 `add_strategy(...)`

## 2.5 可视化监控（dashboard）

### `src/dashboard/order_monitor.py`
- 用途：订单监控 GUI
- 能看到：
  - 订单表（下单成功/失败状态）
  - 成交表（trade 回报）
  - Tick 表（实时行情）
  - 顶部统计（订单数、成交数、拒单数、Tick 数）
  - 事件日志（订单和成交时间线）
- 你怎么用：
  - 默认由 `main.py` 自动启动 GUI
  - 如果只想命令行日志，不开窗口：在 `.env` 加 `ENABLE_GUI=0`

### `src/backtest/`
- 当前状态：占位目录，后续放回测模块

## 2.6 旧版参考目录（非当前主运行链路）

### `algo_trading_system/trade.py`
- 用途：旧版单文件原型（包含连接测试网、轮询行情、策略、下单等）
- 你怎么用：
  - 作为“连接细节参考”
  - 不是当前主系统入口

### `algo_trading_system/builder.md`
- 用途：架构需求说明（你最初给我的设计目标）

### `algo_trading_system/test_*.py / testconnection.py / engine.py`
- 用途：早期调试脚本（代理、连接、密钥测试）
- 你怎么用：
  - 当你怀疑网络、代理、密钥时可单独运行排查

---

## 3. 整个系统怎么用（你按这个来）

## 3.1 第一步：配置 `.env`

在项目根目录 `algo_trading/` 放一个 `.env`：

```env
BINANCE_TESTNET_API_KEY=你的key
BINANCE_TESTNET_API_SECRET=你的secret
BINANCE_PROXY=http://127.0.0.1:7890

TRADING_MODE=BINANCE
TRADING_SYMBOL=BTCUSDT
STRATEGY_TYPE=PULSE_BUY
PULSE_INTERVAL_SECONDS=3
PULSE_BUY_VOLUME=0.0001
GRID_LOWER_PRICE=50000
GRID_UPPER_PRICE=51000
GRID_VOLUME=0.005
```

可选项：
- `TRADING_MODE=SIM`：强制模拟
- `TRADING_MODE=BINANCE`：强制测试网
- 不写 `TRADING_MODE` 时：检测到 key/secret 就自动走 Binance
- `STRATEGY_TYPE=GRID`：使用网格策略
- `STRATEGY_TYPE=PULSE_BUY`：使用高频小额示例策略
- `PULSE_INTERVAL_SECONDS`：示例策略每隔几秒发一笔买单
- `PULSE_BUY_VOLUME`：示例策略每笔买入数量
- `ENABLE_GUI=1`：启动图形化监控页面（默认开启）
- `ENABLE_GUI=0`：关闭 GUI，仅看控制台日志

## 3.2 第二步：一键运行

直接运行 `src/main.py`。

如果 `ENABLE_GUI=1`，会弹出订单监控窗口。

## 3.3 第三步：看日志判断状态

你主要看这几类输出：
- `当前运行模式: BINANCE`：测试网真实下单模式
- `[ORDER] ... status=SUBMITTED`：已提交到测试网
- `[ORDER] ... status=REJECTED ... reject_reason=...`：被拒单，原因在后面
- `[GRID_ORDER]` / `[GRID_TRADE]`：策略已收到订单/成交回报
- `[PULSE_ORDER]` / `[PULSE_TRADE]`：示例策略订单与成交回报

---

## 4. 最常见问题

### 没有下单日志
- 原因：网格上下沿离现价太远，策略条件没触发
- 处理：把 `GRID_LOWER_PRICE / GRID_UPPER_PRICE` 调到接近现价

### 一启动就连接失败
- 原因：key/secret 错、网络不通、代理配置有问题
- 处理：
  - 检查 key/secret 是否是 Binance Spot Testnet
  - 需要代理就设置 `BINANCE_PROXY`

### 想先验证系统逻辑，不碰交易所
- 在 `.env` 写：

```env
TRADING_MODE=SIM
```

---

## 5. 你后续最常做的改动位置

- 改策略逻辑：`src/strategies/grid.py`
- 新增策略：继承 `src/strategies/base.py`，再在 `src/main.py` 里注册
- 改风控参数：`src/main.py` 中 `RiskConfig(...)`
- 改交易所交互细节：`src/infrastructure/gateway.py`
